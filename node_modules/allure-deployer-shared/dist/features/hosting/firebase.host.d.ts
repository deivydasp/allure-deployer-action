import { HostingProvider } from "../../interfaces/hosting-provider.interface.js";
import { FirebaseInterface } from "../../interfaces/firebase.interface.js";
export declare class FirebaseHost implements HostingProvider {
    command: string | undefined;
    private hostedSiteUrl;
    private readonly service;
    constructor(service: FirebaseInterface);
    deploy(): Promise<undefined | string>;
    init(clean?: boolean): Promise<string>;
    private createFirebaseSite;
    deleteAllSites(): Promise<any>;
    private getExistingFirebaseSiteIds;
    private extractSubdomain;
    private hasValidTimestamp;
    private compareTimestamps;
    private deleteFirebaseSite;
}
