import { Notifier } from "../../interfaces/notifier.interface.js";
import { NotificationData } from "../../models/notification.model.js";
import { SlackInterface } from "../../interfaces/slack.interface.js";
export declare class SlackNotifier implements Notifier {
    private readonly slackClient;
    constructor(client: SlackInterface);
    private buildEnvironmentRow;
    private buildEnvironmentBlock;
    private buildStatusBlock;
    private buildButtonBlock;
    notify({ resultStatus, reportUrl, environment }: NotificationData): Promise<void>;
}
