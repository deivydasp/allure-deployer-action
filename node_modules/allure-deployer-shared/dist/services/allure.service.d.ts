import { CommandRunner } from "../interfaces/command.interface.js";
export declare class AllureService implements CommandRunner {
    runCommand(args: string[]): Promise<{
        exitCode: number;
        stdout: string;
        stderr: string;
    }>;
}
