import { Bucket, File } from "@google-cloud/storage";
import { Order, StorageProvider } from "../interfaces/storage-provider.interface.js";
export declare class GoogleStorageService implements StorageProvider {
    bucket: Bucket;
    readonly prefix: string | undefined;
    constructor(storageBucket: Bucket, prefix: string | undefined);
    upload(filePath: string, destination: string): Promise<void>;
    getFiles({ matchGlob, order, maxResults, endOffset }: {
        matchGlob?: string;
        order?: Order;
        maxResults?: number;
        endOffset?: string;
    }): Promise<File[]>;
    download({ destination, concurrency, files }: {
        destination: string;
        concurrency?: number;
        files: File[];
    }): Promise<string[]>;
    sortFiles(files: File[], order: Order): File[];
    deleteFile(fileName: string): Promise<void>;
    deleteFiles(matchGlob?: string): Promise<void>;
}
