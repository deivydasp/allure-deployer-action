import { SlackConfig, SlackInterface } from "../interfaces/slack.interface.js";
import { Block, KnownBlock, WebClient } from "@slack/web-api";
export declare class SlackService implements SlackInterface {
    webClient: WebClient;
    channel: string;
    constructor(config: SlackConfig);
    postMessage(blocks: (Block | KnownBlock)[], text: string): Promise<void>;
}
