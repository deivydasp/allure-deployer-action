import { FirebaseInterface } from "../interfaces/firebase.interface.js";
export declare class FirebaseService implements FirebaseInterface {
    private readonly projectId;
    readonly rootDir: string;
    readonly siteId: string;
    constructor(projectId: string, rootDir: string);
    /**
     * Deploy a Firebase site using the provided configuration.
     * @returns {Promise<void>}
     */
    deployHosting(): Promise<void>;
    /**
     * Create a new Firebase Hosting site.
     * @returns {Promise<any>}
     */
    createSite(): Promise<any>;
    /**
     * List all existing Firebase Hosting sites.
     * @returns {Promise<any[]>}
     */
    listSites(): Promise<any[]>;
    /**
     * Delete an existing Firebase Hosting site.
     * @param siteId ID of the site to delete.
     * @param configPath Path to firebase.json
     * @returns {Promise<void>}
     */
    deleteSite({ siteId, configPath }: {
        siteId: string;
        configPath: string;
    }): Promise<void>;
    private createSiteId;
    createConfigJson(): Promise<string>;
}
